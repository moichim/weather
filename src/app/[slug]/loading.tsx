import { Spinner } from "@nextui-org/react";

const Loading: React.FC = () => <div className="w-full h-[80vh] flex items-center justify-center">
    <Spinner size="lg"/>
</div>

export default Loading;